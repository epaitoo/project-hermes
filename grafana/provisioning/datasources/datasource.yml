apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    # Fixed uid so the committed dashboard JSON can reference it portably.
    uid: prometheus
    access: proxy
    # Service-name DNS on the compose network; unchanged on a VPS.
    url: http://prometheus:9090
    isDefault: true
    editable: false
