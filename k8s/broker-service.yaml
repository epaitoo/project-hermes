apiVersion: v1
kind: Service
metadata:
  name: hermes-broker
  labels:
    app: hermes-broker
spec:
  type: ClusterIP
  selector:
    app: hermes-broker
  ports:
    - port: 8080
      targetPort: http
      protocol: TCP
      name: http
